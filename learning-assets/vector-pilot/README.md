# PP2 Vector Pilot

The 100 SVGs in this kit are arranged by reusable concept, rather than by one workbook or grade. Browse them through the Admin Assets tab or use `catalogue.json` to resolve a stable `assetId`.

Each SVG is self-contained, offline-safe, and suitable for mobile picture choices, matching, counting, colouring, and story scenes.
